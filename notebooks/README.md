# Notebooks

Use this directory for exploration, PDF inspection, chunking experiments, and evaluation analysis.

Keep reusable pipeline logic in `src/` and import it into notebooks. A notebook should not be the only implementation of code required by ingestion, retrieval, generation, or evaluation workflows.

